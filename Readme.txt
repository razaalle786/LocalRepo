# this is my LocalRepo.
# Git branches
    # git checkout -b name  "this will create a new branch."
    # git checkout branch name "is used to move from one branch to other branch." 
    # git branch -d  name "use to delete a branch."
    # to add a feature in a branch 
    # changes made in any brach will remain in that branch. e.g some of these changes will only be visible in this file on github in feature1 branch and not main.
